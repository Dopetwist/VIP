import ScrollReveal from "scrollreveal";

const sr = ScrollReveal({
    distance: "3rem",
    duration: 1000,
    delay: 100,
    easing: "ease-in-out",
    reset: false,
    mobile: true,
});

export default sr;